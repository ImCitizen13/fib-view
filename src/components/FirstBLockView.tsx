import {
  type BlockViewProps,
} from "./types";


export default function FirstBlockView({
  _className,
  // child,
}: BlockViewProps) {
  return (
    <div className={_className} >
      {/* {child} */}
    </div>
  );
}
